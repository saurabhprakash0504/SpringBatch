spring:
   profiles:
      active: local
   datasource:
      url:
      driver-class-name:
   main:
      allow-bean-definition-overriding: true
      banner-mode: off
   application:
      name: spring-batch-example
   batch:
      job:
         enabled: false
